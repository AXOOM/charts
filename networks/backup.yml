feed_uri: http://assets.axoom.cloud/networks/backup.xml
name: Backup Network
summary: network for exposing backup services to central backup system
description: Pulls in the shared "backup" Docker virtual network. This allows the central backup system to connect to individual backup services.
homepage: https://tfs.inside-axoom.org/tfs/axoom/axoom/_git/Axoom.Platform.BaseAssets?_a=readme&fullScreen=true

compose:
  - version: '3.3'
    networks:
      backup:
        external: true

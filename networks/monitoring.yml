feed_uri: http://assets.axoom.cloud/networks/monitoring.xml
name: Monitoring Network
summary: network for Prometheus monitoring
description: Pulls in the shared "monitoring" Docker virtual network. This enables monitoring a service via Prometheus.

compose:
  - version: '3.3'
    networks:
      monitoring:
        external: true

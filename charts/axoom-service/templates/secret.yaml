{{- range .Values.secrets }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .name }}
  labels:
{{ include "axoom.default-labels" . | indent 4 }}
type: Opaque
data:
  {{- range $key, $val := .files }}
  {{ $key | quote }}: {{ $val }}
  {{- end }}
{{- end }}

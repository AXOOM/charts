apiVersion: v1
name: axoom-service
version: 0 # replaced with auto-generated version number by build server
description: runs a service (with monitoring, ingress, etc.)
home: https://github.com/AXOOM/charts/tree/master/charts/axoom-service
